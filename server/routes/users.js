"use strict";

const router = require("express")();

// users
// users/:id --get

router
    .route("/")

    .post((req, res) => {});

router
    .route("/:id")

    .put((req, res) => {});

module.exports = router;
